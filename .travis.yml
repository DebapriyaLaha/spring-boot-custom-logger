language: java
install: true

cache:
  directories:
    - $HOME/.gradle

jdk:
 - oraclejdk8


before_script:
 - chmod +x gradlew
 - chmod +x gradle/wrapper/gradle-wrapper.jar
 - ls -lrth

script:
 - ./gradlew assemble
 - ./gradlew jacocoTestReport coveralls